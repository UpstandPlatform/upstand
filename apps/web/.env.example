# Local-only values. Copy this file to .env.local or run `bun setup`.
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Set this to the API service name when the web server runs in a container.
# UPSTAND_SERVER_INTERNAL_URL=http://server:3000

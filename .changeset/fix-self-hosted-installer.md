---
"server": patch
---

Fix self-hosted installation through `curl | bash`, ensure pinned schedules images deploy correctly with Docker Swarm, make the monitoring image build resilient to restricted Go module proxies, and make the release migrations safe on a fresh database.

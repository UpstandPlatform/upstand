name: Create Stable Release Tag

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: stable-tag-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  tag:
    if: github.repository == 'UpstandPlatform/upstand'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Create release tag when the version advanced
        run: bun run release:tag

      - name: Dispatch Docker release for the stable tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          release_version="$(node -p "require('./package.json').version")"
          gh workflow run "Release and Publish Docker Images" \
            --repo "$GITHUB_REPOSITORY" \
            --ref "v${release_version}"

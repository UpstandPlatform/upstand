global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/upstand-alerts.yml

scrape_configs:
  - job_name: upstand-schedules
    metrics_path: /metrics
    static_configs:
      - targets:
          - schedules:3002
